apiVersion: v1
kind: Service
metadata:
  name: automotive-app-service
spec:
  selector:
    app: automotive-app
  type: NodePort
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30080